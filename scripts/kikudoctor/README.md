# Kikudoctor

This tool is used to diagnose your configuration. It is now exhaustive but it does some basic sanity checks.

To use run or compile it from the repository root:

```bash
# compile
go build -o kikudoctor scripts/kikudoctor/*.go
./kikudoctor
```

```bash
# or run
go run scripts/kikudoctor/*.go
```
