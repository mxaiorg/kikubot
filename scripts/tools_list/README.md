# Utility to list tools

Run options:

```bash
cd scripts/tools_list && go run .          # stdout
go run . -o tools.json                     # write to file
go run . -root /path/to/kikubot            # override repo root
```
