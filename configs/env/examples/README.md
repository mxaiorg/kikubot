# Example environment files

This folder contains example per-agent environment files. When adding a new agent, add a new environment file to the `configs/env/` folder (the parent directory of this folder)

See the [environment documentation](../README.md) for more information on how to name and structure environment files.